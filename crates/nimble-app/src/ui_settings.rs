// Placeholder for a minimal settings dialog (native Win32 dialog).
// This will allow configuring ports, limits, and feature toggles.

#[allow(dead_code)]
pub fn open_settings_dialog() {
    // TODO: DialogBoxParam / CreateDialogParam
}
